# Changelog

## v1.6.1 — 2026-07-23

### 認証不要UIプレビュー
- **UIプレビューページ** (`/ui-preview`): Google認証やGoogle Sheetsへ接続せず、固定ダミーデータでホーム・ライブ入力・試合詳細・ランキングを切り替えて確認できる公開プレビューを追加

## v1.6.0 — 2026-07-23

### UIカンプ再現と昼のスタジアムテーマ
- **昼景スタジアム素材** (`public/images/stadium-day.jpg`): 夜のグラウンド背景を晴れた昼のサッカー場へ刷新
- **ホーム画面** (`grade/[gradeId]/page.tsx`, `MatchList.tsx`): スタジアムヒーロー、仮ロゴ、上部タブ、コンパクトな試合カードへ再構成
- **共通ナビゲーション** (`FooterNav.tsx`, `layout.tsx`): カンプに合わせた濃紺の固定フッターナビとライトグレーの画面基盤へ統一
- **主要画面** (`MatchForm.tsx`, `MatchDetailClient.tsx`, `stats/page.tsx`): ライブ入力、試合詳細、ランキングのヘッダー・余白・カード形状を共通デザインへ統一
- **タイポグラフィ** (`layout.tsx`): 日本語表示用に Noto Sans JP を追加

## v1.5.1 — 2026-07-23

### 戦績・得点ランキングの集計改善
- **得点ランキングフィルタ** (`stats/page.tsx`, `scoring.ts`): 総合・公式・フレンドリー・土曜・日曜・祝日の区分で得点ランキングを切り替え可能に追加
- **祝日判定** (`holiday-jp`): 土日と重ならない日本の祝日だけを祝日ランキングへ集計
- **直近戦績** (`stats/page.tsx`): 「過去1ヶ月」を当日を含む直近30日へ統一
- **試合入力** (`MatchForm.tsx`): ライブ画面の上部を途中経過の「保存」、下部を試合を確定する「保存して終了」に整理
- **UI整理** (`Autocomplete.tsx`, `MatchForm.tsx`): 不要なstate更新と未使用コードを削除

## v1.5.0 — 2026-05-30

### フロントエンドUIをカンプサンプルに合わせてプレミアム刷新
- **グローバルデザインシステム (`globals.css`)**
  - カラーパレット定数（ディープネイビー `#0D1B2A`、アプリグリーン `#00693E`、プライマリブルー `#1565FF`、レッド `#FF2D2D`、ベース背景 `#F5F7FA`）をTailwind themeとして整備
  - アプリ全体の背景色をプレミアムライトグレー（`#F5F7FA`）に固定
- **スタジアム風MATCH CENTERヘッダー (`Header.tsx`)**
  - 背景をディープネイビーからブラックに近いグラデーションに変更
  - ロゴマーク・テキストカラーを白・シルバー基調に調和し、MATCH CENTERの高級感を演出
- **フッターナビゲーション (`FooterNav.tsx`)**
  - アクティブカラーを青からアプリグリーン（`#00693E`）へ変更し、上部にアクティブバーアニメーションを付与
  - 「戦績」タブのラベルをカンプ通りの「ランキング」に変更
  - アイコン形状をカンプデザインと全く同等のモダンな極細線アウトラインアイコンへ置き換え
- **試合カード (`MatchList.tsx`)**
  - 日付、形式、タグの配置をカンプ仕様の3段形式に再構築
  - 中段のスコアを `font-bebas` の巨大文字で中央配置し、勝敗バッジ（WIN/LOSE/DRAW）をスコア真下に角丸ソリッドスタイルで配置
- **ライブ得点入力画面 (`MatchForm.tsx`)**
  - カンプ②のライブ画面に完全刷新
  - 最上部に緑背景の `● LIVE` インジケーターと、白のアウトラインフェーズアクションボタンを配置
  - スコア入力カウンターを左右2カラム（SHARKS青ヘッダー / 対戦相手グレーヘッダー）の白カードカウンターに刷新
  - 得点選手選択用の背番号付きグリッドボタンを構築し、選択中の選手を青背景で強調
- **試合詳細 (`MatchDetailClient.tsx`)**
  - アクティブなタブ（試合記録/基本情報）の下線カラーをアプリグリーンに変更
  - 得点記録リストを、サッカーボール丸型アイコンと「N点」表記の美しく精密なリストに刷新
- **得点ランキング (`stats/page.tsx`)**
  - カンプ④に完全に準拠し、同点時の同順位（例：4位が複数名いる場合など）計算ロジックを搭載
  - 1〜3位の数字を大きく太字で強調した、洗練されたボーダー入りのリストテーブルに刷新

---

## v1.4.0 — 2026-05-30

### UI大規模刷新
- **ホーム画面ヘッダー刷新** (`grade/[gradeId]/page.tsx`)
  - チームロゴ風アイコン（紺丸に白「S」）+ チーム名 + MATCH CENTER サブタイトル
  - 右上ホームアイコン → ギアアイコン（設定ページへ）に変更
- **試合カード刷新** (`MatchList.tsx`)
  - 公式戦バッジ（緑）/ 練習試合バッジ（青）で種別を色分け
  - 日付を YYYY.MM.DD (曜日) 形式に変更
  - カードを上段（種別・日付・形式）/ 中段（スコア・対戦相手）/ 下段（WIN/LOSE/DRAW）の3段構成に刷新
- **ライブ入力画面刷新** (`MatchForm.tsx`)
  - ライブ中はページ最上部にフェーズ表示 + フェーズアクションボタンを集約
  - スコアを大きく最上部に表示（BEBAS NEUE text-6xl）
  - SHARKS / 対戦相手のタブ切替 + 大型 ±ボタンで得点入力
  - 選手グリッドを番号付き3列表示に変更、得点を記録するボタン追加
- **試合詳細画面刷新** (`MatchDetailClient.tsx` 新規, `view/page.tsx`)
  - ヘッダーに「試合詳細」タイトル + シェアボタン追加
  - 「試合記録」「基本情報」タブで情報を整理
  - 得点記録を選手ごとのリスト表示（⚽ 選手名 N点）に変更
  - サーバーコンポーネント / クライアントコンポーネントを適切に分離

---

## v1.3.3 — 2026-05-30

### UI改善・バグ修正
- **戦績ページ GF/GA 表示を日本語化** (`stats/page.tsx`): GF→得点、GA→失点
- **戦績ページ 連勝/連敗ストリーク追加** (`stats/page.tsx`): 直近の連勝・連敗・連続引き分けを表示（2以上で表示）
- **試合詳細 得点者の縦中央揃え** (`view/page.tsx`): ⚽️アイコンと選手名を`items-center`で揃え
- **試合詳細 対戦相手名の uppercase 削除** (`view/page.tsx`): チーム名を大文字変換しないよう修正
- **大会名入力フィールドの自動大文字化を無効化** (`MatchForm.tsx`): `autoCapitalize="none"` `autoCorrect="off"` を追加
- **学年ページのランキングリンク削除** (`grade/[gradeId]/page.tsx`): 戦績ページのTop Scorersで代替

---

## v1.3.2 — 2026-05-30

### UI改善・機能追加
- **スコアフォント太く** (全ファイル): `font-bebas`に`font-black`を追加、合成ボールドで視認性向上
- **フッターナビ変更** (`FooterNav.tsx`): 「ランキング」→「戦績」タブに差し替え
- **戦績ダッシュボード新規作成** (`grade/[gradeId]/stats/page.tsx`)
  - 過去1ヶ月の戦績（W/L/D・勝率・得失点）
  - 今年度公式戦戦績（大会名タグ付き）
  - トップスコアラー上位5名
- **選手ランキングを隠しリンク化** (`grade/[gradeId]/page.tsx`)
  - フッターから削除、試合一覧ページ最下部のテキストリンクでアクセス
  - ランキング=メニュー経由という位置づけに

---

## v1.3.1 — 2026-05-30

### UI改善
- **ダッシュボードのタブ廃止** (`grade/[gradeId]/page.tsx`)
  - 試合入力・選手管理タブを削除。フッターナビで代替
  - 試合履歴のみ常時表示
- **試合入力フォームのスタイル更新** (`MatchForm.tsx`)
  - スコア数字をBEBAS NEUE（6xl）に変更
  - 保存済みトーストをフッターナビの上に移動（bottom-24）
  - フォーム末尾にpb-24追加
- **ランキング画面のスタイル更新** (`players/page.tsx`)
  - 得点数字をBEBAS NEUE（3xl）に変更
  - 1位バッジをamber-400に調整
  - 戻るリンク先を学年ページに修正
  - pb-24追加

---

## v1.3.0 — 2026-05-30

### UI改善
- **フッターナビゲーション追加・Header廃止** (`FooterNav.tsx`, `layout.tsx`)
  - 上部HeaderをフッターナビゲーションBarに完全移行
  - 5タブ: ホーム / 入力 / 試合 / ランキング / メニュー（アイコン＋ラベル）
  - iPhone X 以降の SafeArea（ノッチ）対応
  - gradeId未取得ページではグレードに依存するタブをグレーアウト
- **BEBAS NEUE フォント追加** (`globals.css`, `layout.tsx`)
  - スコア数字に `font-bebas` クラスを適用
  - Google Fonts から自動ロード（`next/font/google`）
- **試合一覧カラー更新** (`MatchList.tsx`)
  - WIN: `#00693E`（緑）/ LOSE: `#FF2D2D`（赤）/ DRAW: グレー に統一
  - 完了試合行の左端にカラーアクセントバー追加
  - スコア数字を BEBAS NEUE に変更
- **試合詳細スコア更新** (`view/page.tsx`)
  - スコア数字を BEBAS NEUE（7xl）に変更
  - 結果バッジの色をデザインパレットに合わせて更新
  - フッターナビ分の余白（pb-24）を追加

---

## v1.2.1 — 2026-05-30

### バグ修正
- **proxy.ts 化によるサーバーエラーを修正** (`middleware.ts`)
  - Next.js 16 で proxy.ts に移行したが本番エラーが発生したため middleware.ts に戻す
- **試合時間の「0」が消えない問題を修正** (`MatchForm.tsx`)
  - `parseInt(value || '0')` → `value === '' ? '' : parseInt(value)` に変更

---

## v1.2.0 — 2026-05-30

### バグ修正
- **試合時間入力のデフォ値が消せない問題を修正** (`MatchForm.tsx`)
  - `value={formData.matchDuration || 15}` → `value={formData.matchDuration ?? ''}` に変更
- **チーム名・会場名の小文字入力ができない問題を修正** (`Autocomplete.tsx`)
  - input に `autoCapitalize="none"` を追加（iOS の自動大文字化を無効化）
- **試合詳細の得点表示に余計な線が重なる問題を修正** (`view/page.tsx`)
  - 詳細情報セクションの `border-t border-slate-50` を削除
- **試合一覧の結果ラベル（WIN/LOSE/DRAW）が左寄せになる問題を修正** (`MatchList.tsx`)
  - `text-center` → `flex items-center justify-center` に変更

### 機能追加
- **LINE内ブラウザで外部ブラウザへ誘導** (`proxy.ts`, `line-redirect/page.tsx`)
  - LINE User-Agent を検出した場合、外部ブラウザで開くよう案内するページへリダイレクト

### その他
- **Next.js 16 対応：middleware.ts → proxy.ts に移行** (`proxy.ts`)
  - `middleware` ファイル規約が非推奨のため `proxy.ts` に変更

---

## v1.1.2 — 2026-04-05

### 機能追加

- **AI分析結果の永続化** (`src/app/api/matches/analyze/route.ts`, `src/components/MatchAnalysis.tsx`, `src/lib/sheets.ts`, `src/types/index.ts`)
  - 分析結果を Google Sheets の `analysis` 列に保存するよう変更
  - ページを閉じて再度開いても分析結果が表示される
  - 再分析時は上書き（「再分析する →」ボタンに変更）

---

## v1.1.1 — 2026-04-05

### バグ修正
- **AI分析のモデル修正** (`src/app/api/matches/analyze/route.ts`)
  - `gemini-1.5-flash` → `gemini-2.0-flash` に変更
  - 無料 API キーで利用可能なモデルに修正

---

## v1.1.0 — 2026-04-05

### 機能追加

- **自動保存（常時）** (`src/components/MatchForm.tsx`)
  - 得点入力・取消時に常に自動保存されるよう拡張（ライブ中以外でも）
  - 自動保存成功時に「保存済み ✓」トースト表示（2秒で消える）
  - `handleRecordGoal` と `handleUndoGoal` の `isLive` 条件を削除

- **保存ボタン整理** (`src/components/MatchForm.tsx`)
  - 「途中保存 (現在のスコアを公開)」ボタンを削除（自動保存と重複するため）
  - `isLive=true` 時のボタンラベルを「試合終了して保存」に変更（動作を明確化）

- **得点ランキング表示機能強化** 
  - 新規ユーティリティ `src/lib/scoring.ts` を作成（`calcTopScorers` 関数）
    - 試合データから得点ランキングを計算する共通関数
    - `playerNames` でフィルタ、`limit` で件数制限可能
  - ダッシュボード試合履歴タブに「TOP SCORERS」セクション追加 (`src/app/grade/[gradeId]/page.tsx`)
    - 上位5名をカード形式で表示（スマホに最適化）
  - 選手管理ページのランキング計算を `scoring.ts` に移行 (`src/app/grade/[gradeId]/players/page.tsx`)

- **AI試合分析機能** (Gemini API 無料枠使用)
  - 新規API: `src/app/api/matches/analyze/route.ts`
    - 試合データを受け取り Gemini 1.5 Flash で分析
    - 試合の振り返り・良かった点・改善点を日本語（150～200字）で生成
    - 事前に GEMINI_API_KEY を Vercel 環境変数に設定が必要
  - 新規コンポーネント `src/components/MatchAnalysis.tsx` (Client Component)
    - 試合詳細ページに「AI で試合を分析」ボタンを配置
    - クリックで分析結果をカード内に表示
  - 試合詳細ページに統合 (`src/app/grade/[gradeId]/match/[matchId]/view/page.tsx`)

### 依存パッケージ追加
- `@google/generative-ai` 版本は npm install で自動取得

---

## v1.0.4 — 2026-03-26

### 機能削除
- **簡易/通常モード切り替えを削除** (`components/MatchForm.tsx`)
  - 使用頻度が低いため入力モード切り替えUIを撤去
  - 常に通常モード（全項目表示）で動作するよう変更

---

## v1.0.3 — 2026-03-20

### バグ修正
- **ログインループの修正** (`auth/signin/page.tsx`)
  - ログイン後に常にトップページへ遷移していた問題を修正
  - `callbackUrl` をURLパラメータから取得するよう変更し、ログイン前にアクセスしていたページへ正しく戻るように対応

---

## v1.0.2 — 2026-03-15

### 機能追加
- **BAND書き出し機能** (`grade/[gradeId]/page.tsx`)
  - 試合履歴タブに「BAND書き出し」ボタンを追加
  - 日付を選択するとその日の全試合を定型フォーマットで自動生成
  - テキストエリアで直接編集可能（コーチ名・マネ名等を手入力）
  - コピーボタンでクリップボードにコピー

### ドキュメント更新
- **マニュアル** (`/manual`)
  - クイック得点ボタン・得点取り消し（Undo）の説明を追加
  - ログイン必須操作と閲覧可能情報のセクション(06)を追加
- **プライバシーポリシー** (`/privacy`)
  - 公開される情報（スコア・得点者名等）のセクション(5)を追加

---

## v1.0.1 — 2026-03-15

### 機能追加
- **得点取り消し機能（Undo）** (`MatchForm.tsx`)
  - 直前に記録した得点を1回だけ取り消せるボタンを追加
  - 対象: クイック得点ボタン・モーダルからの得点記録の両方
  - ライブ中は取り消し後に自動保存
  - ボタンは得点記録直後のみ表示（amber色で視認しやすく）

---

## v1.0.0 — 2026-03-15

### UI / デザイン
- **MatchList: 完了試合をリスト形式に変更**
  - 完了試合：コンパクトなリスト行（結果バッジ + スコア + 対戦相手 + 得点者 + 日付 + 形式）
  - ライブ試合：従来のカード形式を維持
- **結果ラベルを変更**: VICTORY/DEFEAT → WIN/LOSE
- **スコア間の「-」をスコアと同色**（slate-300 → slate-900）
- **試合形式の表示を改善**:
  - "Halves" → "前後半"
  - "15Mマッチ" → 実際の `matchDuration` を使用（例：`8min`）
- **得点者を折り返し全表示**（truncate削除 → break-all）
- **テキストコントラスト改善**（slate-200/300 → slate-400/500）
- **屋外モード機能を削除**（Header.tsx・globals.css から完全除去）

### 機能追加
- **アーカイブページ** (`/grade/[gradeId]/archive`)
  - 直近以外の試合を月別グループで表示
  - 月ごとのW/L/D集計を表示
  - 試合履歴画面に「過去の試合を見る」リンクを追加
- **アクティブ月フィルター**: 試合履歴は当月＋前月（15日以前の場合）のみ表示

### セキュリティ
- **middleware.ts 新規作成**: 未ログイン時は編集ページ・新規入力ページをサインインへリダイレクト
  - 保護対象: `/grade/*/match/new`, `/grade/*/match/[matchId]`
- **ダッシュボードのタブ制御**: 「試合入力」「選手管理」タブをログイン済みのみ表示
- **読み取り専用の試合詳細ページ** (`/grade/[gradeId]/match/[matchId]/view`) 新規作成
  - 誰でも閲覧可能
  - ログイン済みユーザーのみ「編集する」ボタンを表示
  - MatchList・アーカイブページのリンクをすべて `/view` に変更

### マニュアル更新 (`/manual`)
- 画面構成（タブ3種）の説明を追加
- 得点記録フロー（選手選択→ボタン保存）の説明を追加
- アーカイブページの説明を追加
- 「簡易/通常入力モード」の記述を実際のUIに合わせて整理
- セクション番号を05まで拡張

---

## v1.31 以前の主な変更（参考）

- タブ式レイアウト導入（試合入力 / 試合履歴 / 選手管理）
- ライブ試合の得点記録機能（選手選択 → 確認ボタン → 自動保存）
- Google Sheets API 429エラー対策（サーバーサイドTTLキャッシュ）
- 試合フォームのタブ切り替え時に状態が失われる問題を修正
- 連戦機能の修正
- Vercelプロジェクトを `sharks-score-app-detp` に正しくリンク
